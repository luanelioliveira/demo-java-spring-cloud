spring:
  application:
    name: hr-auth-server
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: ${PROFILE:local}

management:
  endpoints:
    web:
      exposure:
        include: '*'