spring:
  application:
    name: hr-worker
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: ${PROFILE:local}

management:
  endpoints:
    web:
      exposure:
        include: '*'