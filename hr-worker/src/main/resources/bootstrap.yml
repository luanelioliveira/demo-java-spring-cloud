spring:
  application:
    name: hr-worker
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: ${PROFILE:dev}

management:
  endpoints:
    web:
      exposure:
        include: '*'